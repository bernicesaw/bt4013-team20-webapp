"""
Django admin configuration for the accounts app.

Currently no models are registered in the admin interface.
Models can be registered here if admin access is needed for debugging or management.
"""
from django.contrib import admin

# Register your models here.
